{
  "name": "<%= props.name %>",
  "version": "1.0.0",
  "dependencies": {
    "evolution-wordpress": "<%= options.dev ? 'master' : '^' + props.evolution %>",
    "wordpress": "<%= props.wordpress %>"
  }
}
